export function utils(): string {
  return 'utils';
}
/**
 * Función que salida al usuario
 * 
 * @param name El nombre del usuario
 * @returns string
 */
export function greet(name: string): string {
  return `Hola ${name}`;
}